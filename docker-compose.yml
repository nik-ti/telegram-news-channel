version: '3.8'

services:
  newsbot:
    build: .
    command: python main.py
    env_file:
      - .env
    restart: unless-stopped
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
    ports:
      - "127.0.0.1:${WEBHOOK_PORT:-8082}:${WEBHOOK_PORT:-8082}"
